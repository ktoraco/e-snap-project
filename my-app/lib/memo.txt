役割: 共通処理やユーティリティ関数を管理するフォルダ。アプリケーション全体で使用される汎用的なロジックを格納します。

共通処理: API呼び出し、データ変換、認証など、UIとは直接関係しないロジックをここに配置します。

ファイル名には注意が必要で、「lib.js」のような曖昧な命名は避けるべきです。例えば、「lib/api.js」や「lib/auth.js」のように具体的な機能を示す名前が推奨されます。